## 1. 协议与平台验证

- [ ] 1.1 固定候选协议版本并检查实例握手、取消、文件创建与路径处理，交付记录源码版本、许可、摘要和适配结论的 `validation.md`。
- [ ] 1.2 在 Linux 验证明文离线普通用户部署及双向工具检查，记录 Shell、架构、基础工具、完整命令和真实文件往返摘要。
- [ ] 1.3 在 macOS 完成同等部署与检查，验证允许的安装位置、执行限制和文件往返，形成独立平台证据。
- [ ] 1.4 在 Windows 验证明文 PowerShell 部署、执行策略与双向检查，确认没有编码命令或策略绕过，并记录真实往返结果。
- [ ] 1.5 用固定候选完成双跳 SSH、tmux、容器和 WSL 的往返及取消验证，核对文件内容和目录结构，记录不兼容组合。
- [ ] 1.6 验证接收器退出、SSH 回退、半帧取消和清理期限后的迟到载荷，证明停止新增发送及输出不可用策略不会泄露数据或自动恢复。
- [ ] 1.7 验证三平台的目录约束、链接竞态、无覆盖发布及 Windows 特殊名称，确认适配器满足文件规格后锁定组件、部署模板与最低条件。
- [ ] 1.8 以大文件、多目录和慢链路压力验证 design 的数据、元数据与时间预算，将采用的固定值及超限结果记录到 `validation.md`，未通过的路径不进入生产实现。

## 2. 领域契约与公共接口

- [ ] 2.1 在 domain 定义本地操作、工具检测、逐文件结果、输出可用性和快照纯数据契约，通过状态与依赖方向测试验证不依赖 runtime、PTY 或 Electron。
- [ ] 2.2 定义 terminal-service 的操作实例接口及 session-runtime 的文件能力注入接口，用公共出口测试证明两个 runtime 不相互引用内部实现。
- [ ] 2.3 在 CONTEXT.md 记录稳定术语，为协议与输出失败策略新增下一编号 ADR，逐项核对契约与架构、安全文档中的边界一致。

## 3. Session 协调与上下文失效

- [ ] 3.1 先建立并发回归，再实现 SessionActor 操作槽，验证本地操作与全部外部写入口互斥，外部审批等待不阻止本地功能。
- [ ] 3.2 实现本地首次写入尝试与终态的 executionContextId 轮换及环境/epoch 失效，验证 C0、操作期间 C1 和操作结束后才通过的旧审批均不能写入。
- [ ] 3.3 实现实例标记、异步前提复核与写入异常终态，测试旧响应、过期文件选择、准备取消无写入及 backend 交付不确定时不重试。
- [ ] 3.4 实现用户接管和有限取消，验证按键先到则下一数据块不发送、按键不延迟重放、已确认文件结果不被迟到取消改写。

## 4. 协议路由与 Sharing

- [ ] 4.1 在普通输出转换前实现有界协议分类，测试任意分片、混合文本、UTF-8 顺序、伪造标记、帧与解压超限，确保两个输出路径均无载荷。
- [ ] 4.2 实现终结等待与输出不可用状态，验证可信完整终结可结束等待，半帧失同步或超时后不保存、不回灌、不自动恢复。
- [ ] 4.3 将 MCP 管线接入统一操作资格和 `SESSION_OUTPUT_UNAVAILABLE`，通过集成测试验证正常传输可 observe、失同步无上下文返回或自动写入、未共享 Session 仍返回过期。
- [ ] 4.4 验证分页、尾部观察、Sharing 起点与跨页脱敏不变，并覆盖重新 Sharing、Token 轮换及迟到标记不能清除输出不可用状态。

## 5. 启用、工具检测与安装

- [ ] 5.1 实现准备与主动启用流程，通过回归验证面板、拖入、未知前台和普通输出不注入命令，首次检测与快照按序执行。
- [ ] 5.2 实现上传/下载独立检测及状态失效，测试同名非兼容工具、仅一侧可用、缺失、执行受限、无结束证据和环境变化。
- [ ] 5.3 实现已验证的三平台固定部署模板和资源装配，通过完整引导检查、来源摘要校验及真实安装验证禁止编码执行和隐式联网。
- [ ] 5.4 实现安装方案引用、完整预览与复制命令，通过测试验证复制不执行、旧方案被拒绝、任意命令和来源不可注入。
- [ ] 5.5 实现有界安装后的复检与准备页返回，覆盖绝对路径、PATH 未更新、版本/权限失败、结果未确认不重装，以及成功不自动传输或复活旧引用。
- [ ] 5.6 验证手工安装后重新检测及适用的可选联网方式，确认完整安装内部行为符合明文规则，缺少第三方审计记录不禁用方案。

## 6. 文件上传下载

- [ ] 6.1 在 Main 实现原生选择与可信拖入的有限引用，测试伪造路径、跨 Session 引用、过期选择和取消回调均不能取得文件权限。
- [ ] 6.2 实现单文件上传的启动、实例响应、流式发送和提交结果，用真实往返校验及结束确认丢失测试验证成功与未确认语义。
- [ ] 6.3 实现单文件下载和本机临时文件发布，测试损坏载荷、本机写入失败、提交竞态及失败后原文件保持不变。
- [ ] 6.4 扩展多文件和目录传输，验证相对结构、符号链接/reparse point/特殊文件跳过、根目录约束与检查后替换攻击均被正确处理。
- [ ] 6.5 实现目的文件系统名称检查，用 Windows ADS、设备名、尾随点/空格、大小写及 Unicode 规范化碰撞用例验证拒绝或冲突行为。
- [ ] 6.6 实现绑定操作的跳过/换名与逐项汇总，验证换名再次校验、等待超时、部分完成保留成功文件及默认不覆盖、不续传。
- [ ] 6.7 实现文件背压、结果分页和应用级资源预算，以超预算文件、目录数量/深度、慢消费者及并行 Session 压力测试验证资源有界。
- [ ] 6.8 实现取消和临时文件清理，验证仅清理可确认归属的资源、目标失效后不发删除命令，并逐项报告未确认结果与已知残留。

## 7. 三平台环境快照

- [ ] 7.1 实现独立快照契约和单次明文采集范围，用实际 OS 与 Shell 分离、混合环境、超时和缺少结束证据测试验证不发布错误快照。
- [ ] 7.2 实现 Linux 的身份、目录、CPU 差分、内存及文件系统磁盘采集，验证 cgroup 限额/宿主机口径与 WSL 数据来源。
- [ ] 7.3 实现 macOS 原生采集，验证指标单位、采样区间、命令缺失和权限不足的逐项结果。
- [ ] 7.4 实现 Windows 未编码 PowerShell/CIM 采集，验证系统能力限制及在 Unix PowerShell 下不会误用 Windows 指标。
- [ ] 7.5 实现启用时一次采样和手动刷新，测试无请求不写入、繁忙暂停、输入后过期、旧标签不改写，以及采集正常收敛不使新样本过期且不恢复 MCP 环境验证。

## 8. 桌面流程与生命周期

- [ ] 8.1 实现有限 IPC、来源与参数校验和状态订阅，通过适配器/preload 测试验证 Renderer 不能取得任意文件、命令、网络或原始协议能力。
- [ ] 8.2 实现 Session 菜单与文件面板，使用浏览器 E2E 覆盖上传、拖入、路径下载、逐项冲突和换名，验证 Header 不增加资源监控。
- [ ] 8.3 实现检测安装流程，使用 E2E 覆盖手动/受控自动检测、预览取消/复制、失败复检、手工安装恢复及成功后返回准备。
- [ ] 8.4 实现快照及操作反馈，使用 E2E 验证采样时间、统计口径、过期/暂停、未确认及输出不可用说明，并检查键盘、主题与窄窗口可读性。
- [ ] 8.5 验证 Session 切换、窗口分离和 Renderer 重载只读取 Main 当前状态，不重启操作、不回放终端历史、不清除输出不可用状态。
- [ ] 8.6 接入 Session 关闭、应用退出和已确认更新清理，用生命周期测试验证流、引用与 PTY 释放，迟到回调无效，用户保存文件保留。
- [ ] 8.7 验证取消 Sharing、停用 MCP 和 Token 吊销仅撤销外部能力，本地传输继续；应用重启不恢复操作、快照或文件授权。

## 9. 交付验证与说明

- [ ] 9.1 在真实 Electron 与 Linux/macOS/Windows 目标执行启用、安装复检、文件往返、快照、接管、失同步与生命周期矩阵，记录实际版本和通过结果，未验证组合明确保留状态。
- [ ] 9.2 更新架构、安全、操作与测试说明，核对三平台最低条件、固定预算、恢复代价和通用审计边界，并完成不含凭据、主机标识或运行日志的 `validation.md`。
- [ ] 9.3 执行 `openspec validate add-terminal-file-transfer-and-status --strict`、显式覆盖本 change 的 Prettier 检查、`pnpm verify`、`pnpm test:e2e` 及适用真实 Electron/打包冒烟，确认命令与行为验证通过。
- [ ] 9.4 逐项核对六份 delta specs、任务和实际 diff，运行 `git diff --check`，只有已实现且取得对应证据的任务才标记完成。
