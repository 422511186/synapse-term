## 1. 语义颜色与对比度基础

- [x] 1.1 为浅色/深色 scheme 的内容、表面和状态颜色定义对比度测试用例，验证正常文字达到 4.5:1、非文字控件边界和焦点指示达到 3:1，并确认现有深色色板不回归
- [x] 1.2 在 Renderer 主题调色板中补齐 scheme 语义颜色令牌与对比度辅助逻辑，运行主题单测验证所有令牌配对满足阈值
- [x] 1.3 为自定义背景/前景/强调色的低对比度组合补充组件与主题测试，验证警告、安全回退和合法保存值保留
- [x] 1.4 在外观设置与主题解析中实现自定义配色的可读性反馈和受影响 UI 安全回退，运行主题相关 Vitest 验证可读组合恢复自定义颜色

## 2. 外部执行状态栏

- [x] 2.1 为外部执行状态补充 Renderer 回归测试，覆盖命令摘要、来源标识、长命令完整内容可访问提示、`started`/`finished` 显隐和 wait 挂起期间持续显示
- [x] 2.2 调整工作区终端结构，使状态栏与 Terminal Session 内容层处于独立布局流，验证长命令不会覆盖终端输出且本地输入路径未被拦截
- [x] 2.3 实现状态栏的 scheme 语义样式、单行省略、窄窗口换行和来源层级，并在状态栏显隐与窗口 resize 后验证 xterm 尺寸重新适配
- [x] 2.4 迁移会话标签外部执行徽标和终端搜索控件到新的语义样式，运行相关 Renderer/E2E 测试确认既有操作和可访问名称保持不变

## 3. 审批卡片

- [x] 3.1 扩展审批卡片组件测试，覆盖浅色 scheme 下的目标 Session、风险分类、风险理由、命令全文、三个裁决动作和超长命令滚动区域
- [x] 3.2 重组审批卡片的标题区、信息区、命令代码区和操作区，保持 `allow_once`、`allow_session`、`denied` 三个裁决值不变，并验证现有组件测试通过
- [x] 3.3 使用 scheme 语义状态令牌重做风险标签、危险动作、焦点和响应式样式，运行浅色/深色视觉验收确认文字和控件边界可读

## 4. 设置工作区

- [x] 4.1 为通用、外观和 MCP 服务区块补充浅色主题可读性与状态区分测试，覆盖正文、辅助说明、输入、Token、审批模式、警告、选中、禁用和危险操作
- [x] 4.2 迁移设置工作区及 MCP/主题设置组件中的硬编码状态色到语义令牌，调整文字层级、卡片边界、控件状态和焦点样式，运行设置相关 Vitest 验证结构和文案保持完整
- [x] 4.3 调整设置工作区响应式布局，验证窄窗口下导航、卡片、输入和按钮不重叠、不裁切、不溢出，并确认设置字段和受限 preload API 不变

## 5. 集成验证与交付

- [x] 5.1 更新 Playwright 场景，覆盖切换浅色/深色主题、触发外部执行状态、长命令不遮挡输出、审批卡片裁决、设置三个分类和自定义低对比度警告
- [x] 5.2 在已加载满足仓库要求的 Node.js 版本环境中运行 `pnpm verify`，确认格式、ESLint、TypeScript 类型检查和 Vitest 全部通过
- [x] 5.3 运行 `pnpm test:e2e` 并检查浅色/深色及窄窗口截图，确认执行期间本地输入可用、Session 状态不变且无文字不可读或布局覆盖问题
