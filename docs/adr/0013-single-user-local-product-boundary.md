# ADR-0013：单用户本地产品边界

状态：已实现

## 决策

当前产品以操作系统用户作为身份和隔离边界，不实现产品账户、共享工作区、集中策略分发或组织级审计收集。

## 当前实现

Core 数据目录、Named Pipe、认证 token、凭据存储和本机文件根目录都按当前用户隔离。MCP 的 shared Session 只是用户显式授权给本机客户端的有限调用边界，不是多用户账户。

## 影响

远程托管、多用户和企业管控需要独立的身份、数据隔离和策略架构，不能从当前本地 Core 自动推导出来。
