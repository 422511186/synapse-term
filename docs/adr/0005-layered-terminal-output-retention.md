# ADR-0005：分层保留终端输出

状态：已实现

## 决策

活动 Session 使用有界内存、终端模型和短期原始日志；长期只保存结构化命令、工具调用、审批、结果和审计，不把完整终端转录当成永久记录。

## 当前实现

`OutputJournal` 提供带 sequence 的输出和 replay，`RetentionManager` 默认清理 24 小时前的原始日志和 30 天前的结构化审计。

## 影响

过早的 replay 游标可能返回 `historyGap`；UI 可用快照恢复显示，但缺失的原始字节不能被解释为完整审计。
